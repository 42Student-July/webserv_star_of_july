name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  googletest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: test
      run: |
        cd config
        make test
