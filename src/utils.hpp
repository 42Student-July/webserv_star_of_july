#ifndef UTILS_HPP
#define UTILS_HPP

#include "ASocket.hpp"
#include "Connection.hpp"
#include "ServerSocket.hpp"

namespace utils {
bool isServerSocket(ASocket *socket);
} // namespace utils

#endif /* UTILS_HPP */
